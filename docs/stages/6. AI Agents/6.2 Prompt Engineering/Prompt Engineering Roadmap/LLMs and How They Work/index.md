# LLMs and How They Work

## Contributor Scope

Explain the minimum model behavior a prompt engineer needs:

- next-token prediction
- tokens and context
- training versus inference
- why outputs are probabilistic
- why prompts shape but do not guarantee behavior

## Build

Create one simple diagram showing prompt -> model -> token choices -> output.
